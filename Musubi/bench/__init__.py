"""Experiment harness — Scenario DSL (YAML), the runner (seed/arm expansion, VCR mode), and
pre-registration (PREREG.md). Drives the three worlds and produces run artifacts (events,
Claims, cassettes, ground truth) for the scoreboard.
"""

from __future__ import annotations
