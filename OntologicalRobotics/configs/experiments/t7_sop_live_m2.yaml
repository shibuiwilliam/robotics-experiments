# T7 SOP検索 — 第2モデル再現（R-A, PROJECT.md §8.3）。課金・要コスト承認。
#
# 第1モデル（t7_sop_live.yaml = text-embedding-3-small）と同一条件で埋め込みスナップショット
# のみ第2版へ差し替え、H4 結果のモデル依存性を切り分ける。第1版はキャッシュ済みで 0 円。
# `orx exp estimate <this>` で概算確認＋承認の上で実行。text_embedding_model は正確な版へ更新。
name: t7-sop-live-m2
task: t7
world_config: configs/world/t2_warehouse.yaml
conditions: [onto-guided, vector-rag]
seeds: [301, 302, 303]
duration_s: 0.0
t7:
  provider:
    mode: openai
    text_embedding_model: text-embedding-3-large   # ← 第2埋め込み版（例）。正確な版へ更新。
