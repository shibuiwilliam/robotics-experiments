# T2 業務‐物理クエリ実験 (H6, H7): OR-reference（表現上限・オフライン）＋
# エージェント3条件（OR-full / B1 / B0）。
#
# 既定は provider.mode=stub（完全オフライン: エージェント条件はハーネス検証のみ）。
# 本計測は mode を openai に変えて実行する — その前に概算トークンの承認を得ること
# （CLAUDE.md §6）。応答は全キャッシュされ、以後は mode=cache で再現できる。
name: t2-business
task: t2
world_config: configs/world/t2_warehouse.yaml
conditions: [OR-reference, OR-full, B1, B0]
seeds: [201, 202, 203]   # 3世界 × 17問 = 51問
duration_s: 20.0
claim_ttl_s: 5.0
t2:
  provider:
    mode: stub
    # llm_model を省略 → 環境変数 ORX_LLM_MODEL または既定 gpt-5.4-mini を使用。
    # 特定モデルにピン留めしたい場合のみここに `llm_model: <id>` を明記する。
    # 解決後のモデル名は構成ハッシュ・マニフェストに記録される。
