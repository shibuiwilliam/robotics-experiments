# T7 SOP検索実験 (H4): オントロジー誘導検索 vs 素のベクトルRAG
# 既定 stub（onto-guided はオフラインで本計測、vector-rag はハーネス検証のみ）。
# vector-rag の本計測は provider.mode=openai（テキスト埋め込み・キャッシュ記録）。
name: t7-sop
task: t7
world_config: configs/world/t2_warehouse.yaml
conditions: [onto-guided, vector-rag]
seeds: [301, 302, 303]
duration_s: 0.0   # T7は物理記録不要
t7:
  provider:
    mode: stub
    # text_embedding_model を省略 → ORX_TEXT_EMBEDDING_MODEL または
    # 既定 text-embedding-3-small を使用（ピン留めはここに明記）。
