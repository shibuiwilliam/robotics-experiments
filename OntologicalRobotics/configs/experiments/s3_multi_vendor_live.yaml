# S3 多ベンダー製造ライン — agent（live）射程（R-B, T10）。課金・要コスト承認。
# 共通オントロジーで正規化した語彙横断の能力契約表が、実 LLM の適格機体選定を助けるかを測る
# （H1/H3 の agent 射程。オンライン較正・故障時再割当は決定的版が担う）。
# OR-full-llm（全ベンダー能力表）/ B1-llm（基準ベンダーのみ・横断翻訳不可）。
name: s3-multi-vendor-live
scenario: s3
world_config: configs/world/s3_multi_vendor.yaml
conditions: [OR-full-llm, B1-llm]
seeds: [301, 302, 303, 304, 305, 306, 307, 308]
duration_s: 0.0
provider:
  mode: openai
  llm_model: gpt-5.4-mini
