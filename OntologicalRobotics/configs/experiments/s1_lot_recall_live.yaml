# S1 ロット回収 — agent（live）射程（R-3a, T8, H2/H6/H7）。課金・要コスト承認。
#
# 決定的版（s1_lot_recall.yaml）と同じ回収列挙タスクを**実 LLM エージェント**が解く。
# OR-full-llm（世界グラフSPARQL＋lot SQL）/ B1-llm（個別生観測＋SQL）/ B0-llm（inline）。
# 実行前に `orx scenario run` がプリフライト（mode=openai でキー検証）を行う。
# 初回 mode=openai で全応答キャッシュ → 以後 mode=cache で 0 円再現。
name: s1-lot-recall-live
scenario: s1
world_config: configs/world/s1_lot_recall.yaml
conditions: [OR-full-llm, B1-llm, B0-llm]
seeds: [101, 102, 103, 104, 105, 106, 107, 108]
duration_s: 18.0
claim_ttl_s: 8.0
provider:
  mode: openai
  llm_model: gpt-5.4-mini   # 課金前に正確な日付付きスナップショットへ更新（§6）
