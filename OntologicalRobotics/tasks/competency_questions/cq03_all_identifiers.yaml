id: cq03_all_identifiers
question: "世界グラフが知っている全ての記号識別子（バーコード）は何か"
sparql: |
  PREFIX orx-upper: <https://orx.local/onto/upper#>
  SELECT DISTINCT ?bc WHERE {
    GRAPH <https://orx.local/id/graph/current> {
      ?e orx-upper:hasIdentifier ?bc .
    }
  }
truth_fn: all_barcodes
compare: set
