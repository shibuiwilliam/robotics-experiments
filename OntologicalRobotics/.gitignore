# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
dist/
build/

# Secrets — APIキーは環境変数からのみ読む (CLAUDE.md §1)
.env

# 実験成果物 (PROJECT.md §11 — runs/ と cache/ はコミットしない)
data/runs/
data/cache/

# macOS
.DS_Store

# 生成レポート（regenerable via `orx report` / `orx scenario run`）。代表は意図的に再生成。
reports/*.md
# REPORT.md も results.json から `make report` で決定的に再生成される（R-INFRA）。
# 追跡すると再生成のたびにツリーが dirty になり provenance を汚すため非追跡（D-1）。
/REPORT.md
# onboard 提案の出力（デモ残骸）
ontology/mappings/proposals/
