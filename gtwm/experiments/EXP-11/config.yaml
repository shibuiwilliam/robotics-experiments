# EXP-11 非機能（遅延、可用性、コスト）。smoke: true で ≤5分の縮小版に切り替わる。
smoke: true
seeds: [0]
n_trials: 3
duration_s: 30.0
probe_config: configs/grounding/probe_train_smoke.yaml
full_run:
  seeds: [0, 1, 2]
  n_trials: 20
  duration_s: 300.0
  probe_config: configs/grounding/probe_train_full.yaml # 本実行時に作成する
  estimated_data: "継続計測。最低1営業日以上のP1運転ログ"
