# Anthropic API key (required for agent-smoke / eval with agents)
ANTHROPIC_API_KEY=

# Central random seed — all randomness derives from this
PSL_SEED=42

# Claude model for agents (pinned for reproducibility)
PSL_AGENT_MODEL=claude-sonnet-4-6
PSL_AGENT_TEMPERATURE=0.0
